# Data Folder

Place the raw Kenya maize production CSV here as:

```text
ken_maize_production.csv
```

The training workflow will copy the raw file to `data/raw_ken_maize_production.csv` and create the cleaned dataset at `data/cleaned_maize_data.csv`.
